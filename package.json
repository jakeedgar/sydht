{
  "name": "sydht.js",
  "version": "1.0.0",
  "description": "An advanced types library, so you don't have to.",
  "main": "index.js",
  "scripts": {
    "test": "test",
    "runPure": "tsc && node dist/pure/index.js",
    "runType": "tsc && node dist/types/index.js"
  },
  "author": "Jake Edgar",
  "license": "MIT",
  "dependencies": {
    "prettier": "^2.7.1",
    "typescript": "^4.7.4"
  }
}
