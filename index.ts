import * as Types from './src/pure/types';
import * as Pure from './src/pure';
import { Nand } from './src/pure/types';
import { Head } from './src/pure';

let arr: Array<number> = [2, 4, 6];

console.log(Nand(2 + 2 === 5, 2 + 2 === 6));

console.log(Head(arr));
